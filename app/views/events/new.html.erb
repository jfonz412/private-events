<% provide(:title, "New Event") %>
<h1>New Event</h1>
<%= form_for @event, url: events_path do |f| %>
	<%= f.label :description %>
	<%= f.text_area :description, placeholder: "Describe your event here..." %>
	<%= f.submit "Submit" %>
<% end %>